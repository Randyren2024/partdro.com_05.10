// Empty analytics utility file - keeping for future use if needed
export const trackPageView = (path: string) => {
  // Analytics tracking removed
};

export const trackEvent = (eventName: string, params: Record<string, any> = {}) => {
  // Analytics tracking removed
};